import React from "react";

import PromoBadge from '@/components/PromoBadge';import { Link as RouterLink } from "react-router-dom";
import slugify from 'slugify';
import { fetchProductDescriptions } from "@/lib/api/products";

const DecorsRecifauxPage = () => {
  return (
    <div className="p-6">
      <h1 className="text-3xl font-bold mb-4">Catégorie : Décors spécifiques récifaux</h1>
      <p>Bienvenue sur la page dédiée à la catégorie "Décors spécifiques récifaux".</p>
      <p>Prochainement : contenu spécifique, produits, fiches conseils…</p>
    </div>
  );
};

export default DecorsRecifauxPage; 